<UserControl x:Class="MediaLibrary.App.Controls.ScanPathCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:MediaLibrary.App.Helpers"
             x:Name="Root">
    <Border Margin="0,0,0,12"
            Padding="14"
            CornerRadius="{StaticResource RadiusCompactCard}"
            Background="{DynamicResource BrushSurfaceAlt}"
            BorderBrush="{DynamicResource BrushBorder}"
            BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel MinWidth="0">
                <TextBlock helpers:TrimmedTextToolTipBehavior.FullText="{Binding Title, ElementName=Root}"
                           Style="{StaticResource LongFieldTextBlockStyle}"
                           Text="{Binding Title, ElementName=Root}" />
                <TextBlock Margin="0,6,0,0"
                           helpers:TrimmedTextToolTipBehavior.FullText="{Binding PathText, ElementName=Root}"
                           Style="{StaticResource LongFieldTextBlockStyle}"
                           Text="{Binding PathText, ElementName=Root}" />
                <TextBlock Margin="0,8,0,0"
                           Text="{Binding DetailText, ElementName=Root}"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource BodyMutedTextStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DetailText, ElementName=Root}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <ContentPresenter Grid.Column="1"
                              Margin="16,0,0,0"
                              VerticalAlignment="Center"
                              Content="{Binding ActionsContent, ElementName=Root}">
                <ContentPresenter.Style>
                    <Style TargetType="ContentPresenter">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActionsContent, ElementName=Root}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentPresenter.Style>
            </ContentPresenter>
        </Grid>
    </Border>
</UserControl>
