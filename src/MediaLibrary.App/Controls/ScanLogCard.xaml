<UserControl x:Class="MediaLibrary.App.Controls.ScanLogCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:MediaLibrary.App.Helpers">
    <Border Margin="0,0,0,14"
            Padding="14"
            CornerRadius="{StaticResource RadiusCompactCard}"
            Background="{DynamicResource BrushSurfaceAlt}"
            BorderBrush="{DynamicResource BrushBorder}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Right"
                        Margin="10,0,0,0"
                        VerticalAlignment="Top">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusText}" Value="已完成">
                                    <Setter Property="Background" Value="{DynamicResource BrushSuccessBackground}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushSuccessBorder}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="部分完成">
                                    <Setter Property="Background" Value="{DynamicResource BrushWarningBackground}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushWarningBorder}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="失败">
                                    <Setter Property="Background" Value="{DynamicResource BrushErrorBackground}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushErrorBorder}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="已取消">
                                    <Setter Property="Background" Value="{DynamicResource BrushWarningBackground}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushWarningBorder}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="扫描中">
                                    <Setter Property="Background" Value="{DynamicResource BrushInfoBackground}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushInfoBorder}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource BadgeTextStyle}" />
                </Border>

                <StackPanel MinWidth="0">
                    <TextBlock helpers:TrimmedTextToolTipBehavior.FullText="{Binding ScanPathDisplayName}"
                               Style="{StaticResource LongFieldTextBlockStyle}"
                               Text="{Binding ScanPathDisplayName}" />
                    <TextBlock Margin="0,6,0,0"
                               helpers:TrimmedTextToolTipBehavior.FullText="{Binding TargetText}"
                               Style="{StaticResource LongFieldTextBlockStyle}"
                               Text="{Binding TargetText}" />
                </StackPanel>
            </DockPanel>

            <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                <Border Margin="0,0,8,8" Style="{StaticResource SmallPillStyle}">
                    <TextBlock Text="{Binding SourceText}" Style="{StaticResource BadgeTextStyle}" />
                </Border>
                <TextBlock Margin="0,2,12,8"
                           Text="{Binding StartedAtText, StringFormat=开始：{0}}"
                           Style="{StaticResource BodyMutedTextStyle}" />
                <TextBlock Margin="0,2,12,8"
                           Text="{Binding EndedAtText, StringFormat=结束：{0}}"
                           Style="{StaticResource BodyMutedTextStyle}" />
            </WrapPanel>

            <UniformGrid Grid.Row="2" Margin="0,6,0,0" Columns="2">
                <TextBlock Text="{Binding ScannedCount, StringFormat=扫描：{0}}" Style="{StaticResource BodyTextStyle}" />
                <TextBlock Text="{Binding NewFileCount, StringFormat=新增：{0}}" Style="{StaticResource BodyTextStyle}" />
                <TextBlock Text="{Binding UpdatedFileCount, StringFormat=更新：{0}}" Style="{StaticResource BodyTextStyle}" />
                <TextBlock Text="{Binding ErrorCount, StringFormat=错误：{0}}" Style="{StaticResource BodyTextStyle}" />
                <TextBlock Text="{Binding IgnoredFileCount, StringFormat=忽略：{0}}" Style="{StaticResource BodyMutedTextStyle}" />
                <TextBlock Text="{Binding DurationText, StringFormat=耗时：{0}}" Style="{StaticResource BodyMutedTextStyle}" />
            </UniformGrid>

            <TextBlock Grid.Row="3"
                       Margin="0,8,0,0"
                       Text="{Binding ErrorMessage}"
                       TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource BodyMutedTextStyle}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasErrorMessage}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <StackPanel Grid.Row="4" Margin="0,8,0,0">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasReasonSummary}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="{Binding ReasonSummaryText}"
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyMutedTextStyle}" />
                <TextBlock Margin="0,4,0,0"
                           Text="{Binding TopReasonSummaryText}"
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyMutedTextStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
